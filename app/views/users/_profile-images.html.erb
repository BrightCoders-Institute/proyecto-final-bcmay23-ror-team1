<div class="images-container mb-4">
  <!-- Show user banner -->
  <% if @user_profile.banner.attached? %>
    <%= image_tag(@user_profile.banner, class: 'banner') %>
  <% else %>
    <%= image_tag("", class: 'banner') %>
  <% end %>

  <!-- Show user avatar -->
  <div class="avatar-container">
    <% if @user_profile.avatar.attached? %>
      <%= image_tag(@user_profile.avatar, class: 'avatar') %>
    <% else %>
      <%= image_tag("", class: 'avatar') %>
    <% end %>
  </div>
</div>
